name: Diagnostic

on:
  workflow_dispatch:

jobs:
  diag:
    runs-on: ubuntu-latest
    steps:
      - name: Probe Yahoo Finance chart endpoint
        run: |
          curl -sS -A "Mozilla/5.0" "https://query1.finance.yahoo.com/v8/finance/chart/7203.T?interval=1d&range=5d" | head -c 4000
          echo
          echo "--- stooq ---"
          curl -sS -A "Mozilla/5.0" "https://stooq.com/q/l/?s=7203.jp&f=sd2t2ohlcv&h&e=csv"
